{
  "name": "steaknstake-subgraph",
  "version": "1.0.0",
  "description": "Subgraph for SteakNStake contract indexing",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio steaknstake",
    "create-local": "graph create --node http://localhost:8020/ steaknstake",
    "remove-local": "graph remove --node http://localhost:8020/ steaknstake",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 steaknstake"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.56.0",
    "@graphprotocol/graph-ts": "0.30.0"
  }
}